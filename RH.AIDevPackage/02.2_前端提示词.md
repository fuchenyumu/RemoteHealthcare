# 前端AI提示词模板（基于实际Vue3+PureAdmin+Element Plus项目）

## 1. 前端架构初始化（基于实际项目）
```text
【前端架构初始化】区域妇幼保健远程诊疗平台-基于实际PureAdmin项目初始化
技术要求：
1. 基于现有PureAdmin项目结构（Vue3.5.22 + TypeScript5.9.3 + Vite7.1.9）
2. 项目结构（已存在）：
   - src/api（接口封装，使用PureAdmin的http工具）
   - src/views（业务页面，基于PureAdmin布局）
   - src/store（Pinia，遵循PureAdmin的store规范）
   - src/components（使用Re系列组件：ReAuth、ReDialog、RePerms等）
3. 路由配置：
   - 基于PureAdmin路由系统（router/index.ts + modules/*.ts）
   - 自动导入路由模块（import.meta.glob）
   - 权限控制集成（useUserStoreHook、storageLocal）
4. 全局工具集成：
   - 使用PureAdmin的http封装（utils/http/index.ts的PureHttp）
   - 集成PureAdmin权限系统（Auth、Perms组件）
   - 统一医疗主题色彩（#165DFF为主色调）
5. UI组件规范：
   - 优先使用PureAdmin的Re系列组件
   - Element Plus + TailwindCSS + Animate.css集成
   - 支持暗色主题切换

现有项目已包含：
- 完整的PureAdmin Re系列组件库
- 完善的权限系统（Auth、Perms、指令）
- 丰富的工具函数（@pureadmin/utils）
- Mock数据系统（mock/*.ts）
- 响应式存储（responsive-storage）

请生成：
1. 医疗业务模块路由（patient.ts、consult.ts、rtc-demo.ts）
2. 医疗业务状态管理（store/modules/consult.ts等）
3. 医疗业务API接口（api/patient.ts、api/consult.ts等）
4. 医疗主题样式配置
```

## 2. 患者信息管理页面（基于实际项目）
```text
【患者管理】区域妇幼保健远程诊疗平台-患者信息调阅页面（基于实际PureAdmin组件）
技术要求：
1. 基于实际PureAdmin项目结构和组件
2. 页面结构：
   - 使用PureTable组件（@pureadmin/table）展示数据
   - Auth组件实现权限控制（仅Doctor角色显示打包按钮）
   - ReDialog组件实现弹窗功能
3. 患者查询功能：
   - Element Plus的el-radio-group切换查询方式
   - el-input输入框，支持姓名+手机号/患者ID两种查询
   - 查询成功后显示患者基础信息（PureDescriptions）
4. 信息展示：
   - 使用el-tabs展示病历信息、检验报告、影像数据
   - 每个标签页使用PureTable展示对应数据
   - 支持表格排序、筛选等功能
5. 权限控制：
   - 仅医生角色显示"一键打包患者数据"按钮
   - 使用useUserStoreHook().roles.includes('Doctor')判断
   - Auth组件包装按钮
6. 接口集成：
   - 使用http工具（utils/http/index.ts）调用接口
   - 错误处理使用ElMessage提示
   - 加载状态使用pure-table的loading属性

现有项目已包含的组件和工具：
- PureTable、Auth、ReDialog、PureDescriptions
- http请求工具、useUserStoreHook
- ElMessage消息提示系统

请生成完整.vue文件（script setup lang="ts"+template+style scoped），包含：
- 权限控制集成（Auth组件）
- PureAdmin组件使用（PureTable、ReDialog等）
- 医疗主题样式（#165DFF主色调）
- 完整的TypeScript类型定义
- 患者数据相关API调用
```

## 3. 会诊管理页面（基于实际项目）
```text
【会诊管理】区域妇幼保健远程诊疗平台-会诊管理页面（基于实际PureAdmin生态）
技术要求：
1. 基于实际PureAdmin的pure-table组件和权限系统
2. 页面布局：
   - 使用ElCard布局（PureAdmin样式系统）
   - 顶部搜索区域 + 表格展示区域
   - 支持表格多选、分页、排序（PureTable功能）
3. 会诊列表功能：
   - 查询条件：状态筛选（el-select）、时间范围（el-date-picker）
   - 表格列：患者姓名、会诊目的、目标医院、状态、操作
   - 状态标签：el-tag显示会诊状态（待审核、已批准、进行中等）
4. 权限控制：
   - 医生可申请会诊、查看自己的会诊
   - 管理员可审核所有会诊
   - 使用Auth和Perms组件控制按钮显示
5. 操作功能：
   - 申请会诊按钮（el-button type="primary"）
   - 查看详情（el-button type="text"）
   - 审核操作（管理员专用，使用el-dropdown或el-popconfirm）
6. 状态管理：
   - 使用useConsultStore管理会诊数据
   - 集成PureAdmin的响应式状态管理
7. 接口集成：
   - http.getConsultList：获取会诊列表
   - http.createConsult：创建会诊申请
   - http.updateConsultStatus：更新会诊状态

现有项目已包含：
- PureTable表格组件
- Auth、Perms权限组件
- ElMessage、ElMessageBox消息系统
- http请求工具
- 路由自动导入系统

请生成完整页面，包括：
- ConsultList.vue（会诊列表页面）
- ConsultApply.vue（申请会诊页面）
- 路由模块配置（router/modules/consult.ts）
- 状态管理（store/modules/consult.ts）
```

## 4. 音视频演示页面（基于实际项目）
```text
【音视频演示】区域妇幼保健远程诊疗平台-音视频会诊演示页面（基于实际PureAdmin组件）
技术要求：
1. 页面布局：
   - 全屏布局（h-screen flex flex-col）
   - 上半区：音视频演示窗口区域
   - 下半区：控制栏（使用el-button-group）
2. 演示窗口设计：
   - 左侧主窗口（flex: 1）：显示"主讲人演示流"
   - 右侧子窗口（w-[300px]）：显示专家流列表
   - 使用静态图片+文字标识（"演示中：省妇幼专家"）
   - 窗口底部overlay显示参与者信息和演示模式标识
3. 控制栏功能：
   - 麦克风控制：el-button图标+文字，点击显示ElMessage提示
   - 摄像头控制：类似麦克风控制
   - 结束演示：el-button type="danger"，点击弹出确认框
   - 演示说明：el-alert显示"当前为演示模式"提示
4. 交互功能：
   - 页面加载时调用http.getRtcDemoConfig接口
   - 结束演示时调用http.endRtcDemo接口
   - 确认框使用ElMessageBox.confirm
   - 成功后跳转至/consult/list（useRouter）
5. 样式设计：
   - 医疗主题色彩搭配（#165DFF为主色调）
   - 暗色背景（bg-black）适合视频演示
   - TailwindCSS响应式设计
6. 权限控制：
   - 仅Doctor和Admin角色可访问
   - 使用Auth组件进行权限验证

现有项目已包含：
- ElMessage、ElMessageBox消息系统
- ElButton、ElAlert等Element Plus组件
- Auth权限组件
- http请求工具
- useRouter路由钩子

请生成完整.vue文件，重点体现：
- PureAdmin组件集成
- TailwindCSS医疗主题样式
- 完整的用户交互流程
- 演示功能的静态实现（无需真实RTC功能）
```

## 5. 系统管理页面（基于实际项目）
```text
【系统管理】区域妇幼保健远程诊疗平台-系统管理页面（基于实际PureAdmin模板）
技术要求：
1. 基于实际PureAdmin的系统管理功能
2. 用户管理页面：
   - 使用PureTable组件展示用户列表
   - 支持用户列表展示、搜索、编辑、删除
   - 角色分配功能（el-select多选）
   - 使用Auth控制操作权限
3. 角色管理页面：
   - 角色列表（PureTable）
   - 权限树形选择器（el-tree）
   - 角色创建/编辑对话框（ReDialog）
4. 操作日志页面：
   - 日志列表展示
   - 时间范围筛选（el-date-picker）
   - 操作类型筛选（el-select）
   - 使用dayjs时间格式化工具
5. 国际化支持：
   - 使用vue-i18n配置
   - 支持中英文切换
   - 所有文本使用transformI18n函数
6. 数据权限控制：
   - 基于useUserStoreHook权限系统
   - 不同角色看到不同数据范围
   - 使用storageLocal缓存权限信息
7. 样式统一：
   - 遵循PureAdmin的设计规范
   - 医疗主题色彩应用
   - TailwindCSS响应式布局

现有项目已包含：
- PureTable、ReDialog、Auth组件
- ElTree、ElSelect、ElDatePicker组件
- vue-i18n国际化系统
- useUserStoreHook权限管理
- transformI18n函数

请生成系统管理相关页面：
- UserManagement.vue（用户管理）
- RoleManagement.vue（角色管理）
- OperationLog.vue（操作日志）
- 路由模块配置
```

## 6. 数据看板页面（基于实际项目）
```text
【数据看板】区域妇幼保健远程诊疗平台-数据看板页面（基于实际PureAdmin+ECharts）
技术要求：
1. 基于实际PureAdmin的数据展示能力
2. 统计卡片：
   - 使用el-row、el-col布局
   - ReCountTo组件显示动态数字
   - 卡片样式：会诊总数、今日会诊、待审核、已完成
3. 图表展示：
   - 使用ECharts（已集成在plugins/echarts.ts）
   - 会诊趋势图（折线图）
   - 科室分布图（饼图）
   - 月度统计图（柱状图）
4. 实时数据：
   - 使用Pinia响应式更新
   - 定时刷新数据（setInterval）
   - WebSocket实时更新（如果需要）
5. 权限控制：
   - 管理员可查看所有数据
   - 医生只能查看相关数据
   - 使用Auth组件控制数据展示
6. 响应式设计：
   - TailwindCSS响应式类名
   - 适配桌面端和移动端
   - 支持暗色主题
7. 国际化：
   - 图表标题、坐标轴标签国际化
   - 使用transformI18n函数

现有项目已包含：
- ReCountTo数字动画组件
- ECharts图表库和插件
- ElRow、ElCol布局组件
- Auth权限组件
- vue-i18n国际化
- Pinia状态管理

请生成Dashboard.vue文件，包含：
- 完整的统计卡片布局（ElRow + ElCol）
- ECharts图表集成
- Pinia数据获取和状态管理
- 权限控制和国际化支持
```

## 7. 表单组件和验证（基于实际项目）
```text
【表单组件】区域妇幼保健远程诊疗平台-表单组件开发（基于实际Element Plus）
技术要求：
1. 基于Element Plus表单组件系统
2. 表单组件：
   - 使用el-form进行表单布局
   - 表单验证规则（el-form的rules）
   - 支持动态表单功能
3. 会诊申请表单：
   - 患者选择（el-select + 搜索功能）
   - 会诊目的（el-input type="textarea"）
   - 目标医院选择（el-select）
   - 紧急程度（el-radio-group）
   - 期望时间（el-date-picker）
4. 患者搜索表单：
   - 双输入框布局（姓名+手机号）
   - 实时验证（@input事件）
   - 搜索建议（el-autocomplete如果需要）
5. 表单提交：
   - 提交前验证（validate方法）
   - 使用ElLoading加载状态
   - 成功后重置表单或跳转
6. 组件复用：
   - 使用ElForm、ElInput等基础组件
   - 支持表单字段的动态配置
   - 表单样式统一化
7. 类型安全：
   - 完整的TypeScript类型定义
   - 表单数据的类型约束
   - 验证规则的类型支持

现有项目已包含：
- Element Plus完整组件库
- ElForm、ElInput、ElSelect等表单组件
- ElLoading加载组件
- TypeScript类型系统

请生成相关表单组件：
- ConsultApplyForm.vue（会诊申请表单）
- PatientSearchForm.vue（患者搜索表单）
- 通用的表单验证工具函数
```

## 8. 权限控制集成（基于实际项目）
```text
【权限控制】区域妇幼保健远程诊疗平台-权限控制系统（基于实际PureAdmin权限系统）
技术要求：
1. 基于实际PureAdmin权限系统架构
2. 角色权限定义：
   - Admin：系统管理员（全权限）
   - Doctor：医生（会诊管理、患者信息）
   - Nurse：护士（部分查看权限）
3. 权限控制实现：
   - 使用useUserStoreHook()获取用户角色
   - Auth组件控制页面/按钮显示
   - Perms组件控制细粒度权限
4. 路由权限：
   - 路由meta中定义roles字段
   - 路由守卫中检查权限（router/index.ts已实现）
   - 无权限自动跳转403页面
5. 数据权限：
   - 后端接口返回权限数据
   - 前端根据权限过滤数据
   - 敏感信息脱敏显示
6. 按钮级权限：
   - 操作按钮的显示/隐藏
   - 禁用状态的权限控制
   - 动态权限加载
7. 权限缓存：
   - 使用storageLocal缓存权限信息
   - 权限变更时自动刷新
   - 登录时初始化权限

现有项目已包含：
- useUserStoreHook用户状态管理
- Auth、Perms权限组件
- router/index.ts路由守卫
- storageLocal本地存储工具
- 完整的权限检查逻辑

请生成权限控制相关文件：
- 权限类型定义（types/permission.ts）
- 权限检查工具函数（utils/permission.ts）
- 权限控制组件封装示例
- 路由权限配置示例
```

## 9. 响应式设计和主题（基于实际项目）
```text
【响应式设计】区域妇幼保健远程诊疗平台-响应式布局（基于实际TailwindCSS+PureAdmin）
技术要求：
1. 基于实际TailwindCSS响应式框架
2. 布局适配：
   - 使用TailwindCSS断点系统（sm、md、lg、xl）
   - 响应式类名（flex、grid、hidden、flex-col等）
   - 移动端优先的设计原则
3. 组件适配：
   - 表格在移动端的处理（水平滚动或卡片化）
   - 表单在移动端的优化（输入框大小、间距）
   - 导航菜单的移动端适配（PureAdmin已实现）
4. 主题切换：
   - 支持明暗主题切换（PureAdmin已实现）
   - 使用CSS变量（:root定义）
   - 主题色彩持久化存储
5. 医疗主题定制：
   - 主色调：#165DFF（医疗蓝）
   - 辅助色：成功绿#52C41A、警告橙#FAAD14、错误红#F5222D
   - 符合医疗行业视觉规范
6. 性能优化：
   - 懒加载组件（defineAsyncComponent）
   - 图片优化和懒加载
   - 代码分割（Vite已配置）
7. 无障碍设计：
   - 键盘导航支持
   - 屏幕阅读器友好
   - 颜色对比度符合标准

现有项目已包含：
- TailwindCSS样式系统
- 暗色主题切换功能
- CSS变量定义
- 响应式断点系统
- 组件懒加载配置

请生成响应式设计相关文件：
- 主题配置文件（style/variables.scss）
- 响应式工具函数（utils/responsive.ts）
- 移动端适配样式示例
- 主题切换组件配置
```

## 10. 开发工具和最佳实践（基于实际项目）
```text
【开发规范】区域妇幼保健远程诊疗平台-开发规范和工具（基于实际项目配置）
技术要求：
1. 基于实际项目开发工具链
2. 代码规范：
   - ESLint + Prettier集成（项目已配置）
   - TypeScript严格模式（tsconfig.json已配置）
   - 组件命名规范（PascalCase）
   - 文件组织结构（遵循PureAdmin标准）
3. 组件开发规范：
   - 使用Composition API（<script setup>）
   - Props和Emits的类型定义
   - 组件文档和示例
   - 单元测试要求（Vitest + Vue Test Utils）
4. 状态管理规范：
   - Pinia Store模块化
   - 状态持久化策略（responsive-storage）
   - 状态变化的可追踪性
5. 性能优化：
   - 组件懒加载（defineAsyncComponent）
   - 图片优化（Vite图片处理）
   - 打包优化（Vite构建配置）
6. 调试工具：
   - Vue DevTools集成
   - TypeScript支持
   - 错误边界处理
7. CI/CD集成：
   - 代码质量检查（ESLint、Prettier、Stylelint）
   - 自动化测试（Vitest）
   - 构建和部署流程

现有项目已包含：
- 完整的ESLint、Prettier、Stylelint配置
- TypeScript严格类型检查
- Vitest测试框架
- Vite构建工具
- Husky + lint-staged提交前校验

请生成开发规范文档：
- 代码风格指南（基于现有ESLint配置）
- 组件开发模板（基于<script setup>规范）
- 状态管理最佳实践（基于Pinia + 持久化）
- 性能优化指南（基于Vite + 懒加载）
```

## 11. 特殊业务场景（医疗行业专用）
```text
【医疗专用场景】区域妇幼保健远程诊疗平台-医疗行业特殊需求实现
技术要求：
1. 患者隐私保护：
   - 敏感信息脱敏显示（姓名显示为"张**"，手机号为"138****8000"）
   - 数据访问日志记录
   - 操作权限精细控制
2. 医疗数据标准：
   - 支持HL7、DICOM等医疗数据标准
   - 医学影像数据展示（支持DICOM查看器）
   - 检验报告标准化显示
3. 会诊流程控制：
   - 多方会诊权限管理
   - 会诊记录完整性保证
   - 电子签名和盖章功能
4. 音视频质量保证：
   - 网络质量检测和提示
   - 降级方案（音频-only模式）
   - 录制功能和质量选择
5. 移动端适配：
   - 医生移动查房支持
   - 语音输入病历功能
   - 离线数据缓存
6. 安全合规：
   - 数据加密传输
   - 操作审计日志
   - 等保2.0合规要求

请基于实际PureAdmin项目实现：
- 患者信息脱敏组件
- 医疗数据展示组件
- 会诊流程管理组件
- 移动端适配样式
- 安全审计日志组件
```

---

## 使用指南

### 基于实际项目的开发流程
1. **利用现有组件库**：优先使用PureAdmin提供的30+ Re系列组件
2. **遵循项目规范**：严格遵循现有项目的TypeScript、ESLint配置
3. **集成权限系统**：充分利用已有的Auth、Perms组件和路由守卫
4. **使用工具函数**：广泛使用@pureadmin/utils工具库
5. **保持样式一致**：遵循TailwindCSS + PureAdmin样式系统

### 医疗行业特殊考虑
1. **隐私保护**：患者信息脱敏、数据加密
2. **合规要求**：等保2.0、数据安全法
3. **业务流程**：会诊申请→审核→执行的完整流程
4. **数据标准**：支持HL7、DICOM等医疗标准
5. **多终端适配**：PC端、平板端、移动端全覆盖

### 技术栈版本（基于实际项目）
- Vue: ^3.5.22
- TypeScript: ^5.9.3
- Vite: ^7.1.9
- Element Plus: ^2.11.4
- PureAdmin: ^2.6.2
- TailwindCSS: ^4.1.14
- Pinia: ^3.0.3
- @pureadmin/utils: ^2.6.2

通过基于实际PureAdmin项目的AI提示词模板，我们能够更高效地生成符合项目规范、具备完整功能的医疗平台前端代码，确保开发的代码质量和一致性。

